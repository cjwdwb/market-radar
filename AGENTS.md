# Market Radar — Agent 工作约定

本文件只放共享规则；项目事实见 docs，不复制到各角色。用户明确任务优先。
本次体系配置不授权发布、修改凭据或更改产品功能。

## 最小上下文
- 开始任务：读取 tasks/ACTIVE.md；首次进入项目或上下文缺失时读 docs/PROJECT.md。
- 架构或跨模块工作：按需读 docs/ARCHITECTURE.md、相关 DECISIONS.md 条目。
- API、行情字段、认证或监控协议变化：读 docs/API_CONTRACTS.md。
- 接续任务：读 docs/CURRENT_STATE.md；UI 工作按需读既有 mobile-design.md、motion-design.md。
- 已在当前上下文且未改动的文件不重复读取；先用 git status/diff 判断变化。
- 文档与代码冲突时核对相关实现，明确差异；不要把历史说明当作当前事实。

## 搜索与修改
- 先用 rg 在任务给定目录/符号做 targeted search，再读命中附近片段。
- 不无目的扫描全仓；首次定位失败才逐步扩大搜索并说明理由。
- 默认跳过 node_modules、vendor、build、dist、.sites-runtime、锁文件和大型无关文件。
- 限定任务允许路径，保持小 diff；禁止无关重构和顺手改需求。
- API contract、依赖、数据库 schema、认证边界不得随意修改。需要超出 scope 时先记录原因和最小扩展；不推定新授权。
- 保留用户已有未提交改动，不覆盖或回滚他人工作。

## 角色与流转
- PLANNER：目标、非目标、允许路径、接口影响、验收和测试写入任务卡；不写产品代码。
- BUILDER：按任务卡实施和测试；交接只写变更路径、验证结果、风险及下一步。
- OPTIMIZER：仅在有性能/体验目标或可验证瓶颈时进入；记录前后证据，不改需求。
- AUDITOR：默认只报告不修复；先看 git diff，再追踪必要调用方、契约和测试。
- 常规顺序：PLAN → BUILD → 相关测试 → 按需 OPTIMIZE → AUDIT → 必要修复/复核。
- 小型低风险任务可合并规划与实施，省略无收益优化；不强制启动四个 Agent。
- 审计结果按严重度列文件/行、触发条件、影响和证据；无发现也注明未覆盖范围。
- Skills 是角色工作流，调用本身不自动启动四个独立 Agent。

## 并行与交接
- 仅互不依赖且写入路径不重叠的任务可并行；每个写入 Agent 使用独立 worktree/分支。
- 分支默认 codex/<task-id>-<role>；禁止多个 Agent 同时写同一工作树。
- tasks/ACTIVE.md 由协调者单写。并行任务卡使用 tasks/<id>.md，并显式传入子任务路径。
- 派发最小任务卡与必要路径，不复制完整聊天、所有 docs 或日志。
- 接口确定后再实施依赖方；同一文件改动串行；最终审计针对固定版本/稳定 diff，修复后复核。
- 完成后协调者将任务卡归档到 tasks/archive/<id>.md，保留结果和测试摘要，不堆积完整日志。

## 验证
- 优先运行受影响测试，具体命令见 docs/PROJECT.md。
- npm test 会先构建；只在集成风险、构建依赖测试或项目要求时运行完整检查。
- 不重复运行已通过检查，除非新增改动或新证据要求；文档任务核对路径、事实、格式与 diff 即可。
- 审计先查工作区 diff、暂存 diff、未跟踪文件；已提交任务用任务卡指定基线比较，不能仅看空的 git diff。
- 完成汇报区分“已验证”“未运行”“待验证”，不把历史测试或目标描述写成当前通过。

