# MR-WATCHLIST-INFORMATION-HISTORY-27

2026-09-22 · B0 isolated foundation + C0 watchlist locally verified; complete2.7 remains open. Coordinator owns ACTIVE.

## Baseline and authority

- Re-read branch main, HEAD `00d7ef41cdfccccec32674cee6f830f6b812a45e`; clean tracked/staged/untracked state at entry. Work branch `codex/mr-watchlist-information-history-27`.
- Published product is 2.6 / Sites v24, source `8641d8193e5e2c27fb316bcaed9b977d63e1d119`, per MR-PUBLISH-STATE26. This round has not probed production or re-tested login. Prior QA limitations remain historical, not a new gate.
- User authorizes investigation, code and isolated local tests. No purchase, paid resource creation, production migration/write/backfill, continuous scheduling, secret/access changes or push/merge/tag/deploy.
- Applied existing market-planner; readonly GPT domain and storage inventories completed. DS actual calls: 0; no suitable authorized external task required.

## Goal / non-goals

Four required release tracks remain: same-source watchlist state; real attributed asset information; queryable September archive; reproducible research with distinct reconstructed/live/forecast identities. None may be silently dropped to call 2.7 complete.

No trading, scores, calibrated probability claims, LLM, full market scan, replacement of live sources, new account system, framework or dependency. New historical capabilities do not relax live gates.

## Actual capability and gaps

Browser `market-radar-preferences-v1` owns watchlist (max20); defaults and optional monitor's manually synchronized list do not prove current owner universe. Actual user list is not available to this session; requested explicitly, never inspect private browser sessions. Helper benchmarks follow existing explicit engine mapping and are not user assets.

Parent app owns quotes/trends/clock. buildAssetStateV2 currently derives selected only. Watchlist can derive at most20+selected from the same loaded snapshot, with selected consuming the same result; no new live requests/timer. Public visitors' browser choices must never mutate backend collection universe.

Site hosting config has d1:null, db/schema.ts empty. Separate monitor SQL has settings/alerts/runtime only; runtime overwrites latest report. Binding types/schema are not cloud resource proof. Existing Node24.19 built-in SQLite and monitor tests enable isolated file DB without dependencies. Do not run monitor seeds (enabled=1/default assets).

## A — six frozen minimum contracts

Detailed source/permission evidence, schema semantics, budgets and research protocol: `docs/HISTORY_FOUNDATION.md`. These contracts approve local fixture foundation only while source/universe gates are unresolved.

1. Universe/source: owner-explicit list, canonical identity including market/venue/provider symbol/currency/adjustment; versioned separately from browser state. Empty until provided. SEC metadata candidate only after official access/rights and exact CIK confirmed. OKX/Yahoo historical persistence/display NOT approved merely by current live access. Source approval is use-specific and explicit, not HTTP200.
2. Time/provenance: target [2026-09-01 market-local start, frozen batch cutoff); crypto UTC. Original timestamp precision retained. Separate event/bar time, source availability/version, actual received time, calculation time. No fabricated observedAt/fetchedAt. Fixed read revision prevents pagination mixing later revisions.
3. Storage/access: new isolated SQLite file under ignored work/state27, additive tables, no monitor modification or remote binding. Local process owner only; no HTTP management interface yet. Atomic page facts+checkpoint, immutable revisions; reads scoped to owner/universe and fixed revision. No product access-code-to-admin escalation.
4. Collection/coverage: explicit bounded manual runner, no imports/GET/visitor-triggered fetch. Finite request/page/byte/write/time limits, concurrency1, bounded retry, checkpoints, no-progress/repeat-page rejection and stop. Complete means endpoint traversal only unless source-specific expected-grid validation proves otherwise. Unknown calendar = unknown completeness.
5. Replay/research: separate historical validity adapter; reuse directionForWindow/volatilityForWindow without making live Quote. Current-vintage descriptive reconstruction first, point-in-time unavailable without version-publication proof. Same-asset short90 features, +30m outcome, full feature/target separation and sample count gate; no predictions invented or backdated. See protocol in document.
6. Delivery/verification: A evidence review before B; B0 isolated fixture storage/runner/restore permitted now. Real B requires frozen list/source/use/budget/target. Source B success immediately enables matching C timeline; watchlist UI and D replay require own stable contracts/audit and cannot be reported finished now. Full version stays open until actual information/archive/research delivery.

## Slice boundaries / allowed paths

- A now: task/ACTIVE, this card, docs/HISTORY_FOUNDATION.md; source extracts only ignored work/state27/sources. Readonly official docs allowed; do not bypass HTTP403 or certificate failures.
- B0 now: `collector/schema.sql`, `collector/store.mjs`, `collector/runner.mjs`, `scripts/history-local.mjs`, `tests/history-foundation.test.mjs`; isolated fixtures/tests in existing test framework. No network provider enabled. Manual local init/status/export/restore/query CLI; no arbitrary remote URL, production D1 or HTTP server. Exact interfaces in contract.
- B real next: reviewed source adapters under collector, same tests; task addendum must record actual approved universe and source rights, frozen cutoff, local persistence and numerical batch limits before real collection.
- C next after relevant B evidence: lib/radar/watchlist-state.ts, app/market-radar.tsx, components/radar watchlist/timeline/history, app/radar.css, existing tests/browser/radar.mjs and node tests. No invented data UI, source change or reminder semantics.
- D next: separate lib/radar/historical-state.ts / collector research storage and tests; pure helper extraction only if necessary, entire 2.6 output equivalence first. No actual research result until source/version rules qualify.
- Do not alter package/lock, monitor/worker/schema, access control, production configuration, model provider or original financial thresholds.

Only coordinator writes this worktree; readonly agents have no writes. Any later parallel builder gets independent branch/worktree and non-overlap card after interfaces freeze.

## B0 acceptance / tests

New schema isolated; no seeds/auto-enable. Immutable same-key revisions with canonical hash, retry equal to current head idempotent; A→B→A appends a new revision; owner isolation; fixed-cutoff/read revision; facts+cursor in transaction. Crash before commit leaves no facts/cursor; after commit resume safe. Requests charged before dispatch; restart retains consumed budget. Repeated cursor/page/429 bounded; stop/cancel durable. Query dates/page size bounded and stable; no network from query. No claim of gap-free coverage from first/last timestamps.

Cross-process close/reopen persistence; export to manifest-checked portable snapshot, restore into new empty DB only, compare keys/content/checkpoints/query; tampered export rejected. Fixture identity explicit and cannot be stored as observed_live. Source fields and safe HTTPS links only; no raw external HTML or secrets. Production true samples remain BLOCKED.

Run affected Node tests, then full Node/typecheck/build according actual package scripts; no duplicate wrapper runs. Relevant lint. No UI code in B0 => new browser UI scenarios NOT RUN, not PASS; original test harness stays loopback only. Measure SQLite query/export size/runner counts under fixed fixtures, not cloud costs or phone FPS. Optimizer only measured bottleneck.

## Gates and pending input

BLOCKED for real B: current owner watchlist; approved history source persistence/research/display scope; source samples/access. SEC docs returned403; OKX agreement TLS/certificate check failed. No circumvention. Asked user for list and whether compliant SEC User-Agent exists (variable name only; no secrets requested). No licensed source approved yet. Yahoo archival rights/retention unknown; macro series/vintage approval deferred.

Cloud is separately NOT AUTHORIZED: no production resource/DB identity assumed, no migration, scheduler, long-term backup destination or spend approved. Local files are development evidence, not durable off-machine archive.

Independent GPT A contract audit required before B0. B0 stable diff then independent storage/authority audit, Builder fixes findings and Auditor VERIFY. Final whole2.7 audit remains pending until B–D implemented; do not archive as complete.

## Execution evidence

### Continuation — unblock C0 while actual B sources remain gated

User asks “想办法解决”. Preserve all B0 uncommitted files and final hashes. Fresh readonly planning confirms Watchlist Intelligence and explicit browser-list export do not depend on archived data/source rights. This is an independently deliverable C0, not permission to claim real B or whole2.7 finished. Old QA remains out of gate; no purchase/cloud/production/push/deploy authorization inferred.

Allowed added paths: lib/radar/watchlist-state.ts, lib/radar/watchlist-export.ts, components/radar/watchlist-state.tsx, components/radar/radar-feed.tsx, app/market-radar.tsx, app/radar.css, tests/radar-watchlist27.test.mjs, tests/browser/radar.mjs; task/ACTIVE/HISTORY_FOUNDATION and ignored outputs/state27-watchlist evidence. Root is sole writer; readonly GPT source and UI inventory completed. Existing B0 files must remain unchanged in C0. Integration follow-up permits tests/ui-components.test.mjs only to supply the new required shared-map/export props in its existing SSR fixture; no existing assertion removed.

Contract: parent derives same buildAssetStateV2 objects once for current valid unique watchlist(max20)+selected(max1extra), from existing snapshot/clock/online/enabled; Classic/Radar selected consume Map entry. No new API/provider symbol/timer, no financial threshold or State rule edits. Each watchlist row shows per-dimension availability for direction,RMS,relative,alignment,transition (90/180 horizons separately where applicable), asset evidence end/receipt context; absent values notneutral. Default list order; native details for complete labels/reasons and existing viewAsset navigation for chart/manual range. No cross-market scores.

Export browser-watchlist-v1: user click only, local Blob download; current React list notfiltered/sorted UI list, no alerts/settings/price/history/account/cookie. exportedAt is actual click time, source browser_watchlist, identity unresolved pendingownerconfirmation. App market mapping and source route declared, actual venue/CIK/adjustment unknown null, no fabricated archive canonicalidentity or licenseduniverse. Tracks whether list came from saved browser preferences, defaults, storage failure or session edits; hydration required. No automaticserverupload/collectorstart. Empty export allowed with clear empty list.

Tests: same-object Map reuse and wholeV2 equality;20+1bound,duplicates,missing/stale/offline/paused/benchmark failure; export order/unknown/noalias/no personalextras. Existing loopback harness new RADAR_WATCHLIST27_ONLY flag, all5viewports; keyboard/details, selected/manualrange,Watch/remove/undo,persistence,downloadparse,partial/empty/stale and3motionoptions. Isolated fixed-clock navigation/expand/export request delta0 excluding explicitasset/rangechange andnormalpolls. FullNode/build/typecheck/affectedlint, oldSTATE26browser suite, independent GPTaudit/FIX/VERIFY. Local screenshots are fixture, notrealownerdata orproduction.

Sources investigation may continue lowfrequency official documents only. SEC403 explicitly rate/automated-access policy; do not varyidentity/proxy orretryprotecteddata. DefaultTLSalternativeclient may diagnose connection failures, neverdisablecertchecks. Actualuseruniverse, source rights, budgets andrealB remainneeded; C0export removesmanualentryfriction, doesn'tpretendwealreadyreadtheuserlist.

A independent GPT audit:1 MEDIUM old-content reversion and1 LOW unbounded export contract; amended head-only dedupe, A/B/A test,16MiB/10000row limits and durable rejected-response byte accounting; independent VERIFY PASS before Builder.

B0 implemented schema/store/fixture runner/local CLI. Implementation review3 MEDIUM fixed: SQLite null-prototype export, day-publication invented revision/time, restore accepted invalid IDs/terminal budgets. Added negative tests. Main web/financial/monitor/auth/config files untouched. Real sources disabled structurally, not merely instruction.

Final checks: `node node_modules/vinext/dist/cli.js build` PASS (`outputs/state27/build.txt`); `node scripts/typecheck.mjs` PASS (`typecheck.txt`); `node --experimental-strip-types --import ./tests/register-types.mjs --test tests/*.test.mjs` 246/246 PASS (`node-tests.txt`); affected ESLint PASS.28 new tests include subprocess crash, restart and exportrestore. npm test wrapper not invoked. No UI change/browser rerun, no claim of2.7UI acceptance. Final1000fixturebars: write70.16ms/queryp95(200rows)1.227ms/restore69.78ms/DB671744bytes/backup598999bytes; `measurement.json`. Mainclient759428bytes same2.6record. No Optimizer gate triggered.

One budget fixture initially failed because preceding case already stored its facts, correctly reducing subsequent inserted writes; separated synthetic sources per case, unchanged assertion then full suite PASS. No product-rule test weakened. Historical2.6 results distinct from new regression execution.

Final independent B0 audit: storage2MEDIUM (repeat-page hash included received sort; mutable asset identity), runner3MEDIUM (concurrency, resume retry limits/Retry-After, cancellation during backoff). Fixed all; successor lease fencing required secondfix. Storage27tests+restore probes VERIFY PASS; runner7targeted+2extra probes VERIFY PASS on finalguard. Product hash manifest final; no remaining confirmed findings in bounded B0. Whole2.7 is not accepted; task stays open, not archived.

Yahoo official US terms now retrieved, restrict automated collection without express priorpermission; historical rights still unapproved. D1 limits/local docs retrieved: Paid10GB/Free500MB perDB,30/7dayTimeTravel; no accountplan/cloudresource verified. Source/publication matrix and six contracts in document. User's true list and optional SEC UA variable remain pending; no response fabricated as consent.

| Track | C0 checkpoint status (superseded for B1/C1 below) |
| --- | --- |
| Code/local verification | B0 PASS; C0 PASS253Node/build/typecheck/14new browser+10old; final independent audit VERIFY PASS; not all2.7 code |
| Real owner universe | BLOCKED, browser list not provided/read |
| Actual information source chain | Board official macro rights/sample verified; real collector DB/query/UI chain NOT RUN. SEC403 retained; no circumventing |
| September backfill | BLOCKED,0 actual rows collected; no licensed price source approved |
| Query/persistence/restore | PASS on fixture, cross-process; historical math reproduction Deferred D |
| Watchlist UI/timeline/history UI | Watchlist C0 PASS on five desktop-simulated viewports; timeline/history UI NOT RUN, realB dependency open |
| Research protocol | Defined; implementation/results NOT RUN, no prediction record invented |
| Cloud resources/production migration/continuous collection | NOT RUN / not authorized |
| Production | Existing2.6/Sitesv24; this round NOT DEPLOYED, no push/tag/merge |

## C0 execution and new source finding

## Continuation B1/C1 — approved Board macro local chain

2026-09-22 user “继续完善”; Planner rechecked HEAD00d7ef4/currentbranch and preserved17prior files. No production authorization inferred. Independent readonly runtime inventory confirms Worker APIs/D1 boundary, no configured hosting D1/R2, SQLite constructor performs DDL. Choose explicit local public-view import into existing Radar rather than an HTTP middleware bypassing Worker gate. No new service, dependency, appAPI, Worker, auth, D1/R2, live price source or financial rules. This is local/offline macro context, not per-asset company coverage or completed2.7.

Frozen B1 source profile: fed-board:monetary-v1; actual Board-owned monetary RSS metadata only, fixed HTTPS www.federalreserve.gov/feeds/press_monetary.xml, redirect refused. Approved public-domain attribution evidence and real sample above. Source URLs restricted to /newsevents/pressreleases/monetaryYYYYMMDDa.htm style Board article IDs. No arbitrary URL, private preferences, images/logo, full article copying, external-model disclosure or causal financial interpretation. Namespace context:fed-monetary, owner local-public-macro is a fixed public local operation scope, not real user universe. No asset/benchmark mapping; macro context is explicitly general. Fixture remains separately guarded.

Time: start2026-09-01T00:00:00Z; cutoff frozen at batch creation, <=31dayrange. Actual first receipt on successful response/commit; source publication from RSS RFC timestamp, minute precision when seconds zero. Current-vintage reconstructed only, unknown revision knowledge/PIT; no observed_live or fabricated old receipt. Changes retain head-only content versions incl A/B/A. Latest RSS endpoint traversal never proves allSeptember completeness; report selected in-range count, ignored out-of-range count, no expected denominator.

Storage: reuse ArchiveStore transaction/checkpoint/lease/retry/export/restore engine, strict single Board profile added. New physical schema2 only; reject old schema1 before DDL, close connection, no automatic migration or overwrite. Old fixture logical v1 exports remain restorable after validation; real-source exports use logical v2, cannot enter v1 fixture identity. Existing fixture rules, counters, source rejection and crash tests preserved. Real metadata only information kind, never bar. New real archive file fixed work/state27/fed-monetary/archive.sqlite; test files isolated. Collection is explicit Node CLI command, never GET/page/import. Shared durable per-source ledger within fixed real DB: max6requests/rolling24h, minimum1s between source dispatches, plus perrun3requests/1page/256KiBpage and total/50writes/30s, concurrency lease; stop state persisted, no unlimited retry. CLI does not accept custom URL/source/owner/budget, nor cloud paths. At most2real feed reads for initial integration and idempotence verification, only if current request ledger allows. Automatic continuous schedules absent. B1 cost0subscription/cloud, <=512KiB actual network target for2samples; local artifacts not permanent/off-machine retention.

A contract FIX: Source request ledger + lastDispatch + global Retry-After + source-wide lease checked/reserved atomically with per-run budget; multiple runs/connections cannot bypass. Failed/aborted/timed-out dispatch remains charged. v2 backup includes verified source ledger, but restored databases have persistent collection_disabled state: query/export only, cannot reserve real requests or regain budget. CLI fixed canonical collection path cannot overwrite/init/reset; restore only new different target. Future disaster recovery enabling collection requires a separately reviewed non-rollback ledger protocol. Initial integration cap2 means total actual HTTP dispatches including retries; each actual CLI sample run uses requests=1. Physical schema2 and fixture logical-v1 are distinct; v1 restore rejects every real-source config/fact/ledger. Add cross-run429/restart and restored-collection denial tests.

B1 first actual dispatch got200/9645bytes XML but parser rejected,0facts; ledger retained. Planner permits a fixed ignored local diagnostic source artifact for the remaining approved request: Board RSS metadata body only (not article), actual receivedAt/byte/hash, never public-view/UI or Git. This is provenance/debug evidence within approved source rights; parse/commit times remain distinct. Existing cap2 still applies until explicit documented change.

Public view contract fed-monetary-view-v1: derived from DB bounded latest revisions; excludes owner/runconfig/path/userpreferences/rawHTML. Contains source policy/attribution, exportedAt, frozen range/readRevision, records with canonical sourceID/title/type/published precision/actual received/contentversion and conservative coverage. Max1MiB/200records, immutable fixed view identity from validated checksum; checksum is corruption detection, not source authentication. Native safe-HTTPS links only. UI file input is untrusted: bytes/field/row/time/duplicate checks before use, text-only rendering. Explicit “本地归档 / 未在线重新核验”; no persistence to localStorage/no upload/no new network. Bounded UTC date filter and fixed-snapshot pagination(5perpage); date/file changes reset page, no mixing snapshots. Local filter empty means no matching archived records, not no events. Import only after explicit action; closed-by-default section in existing Radar, independent of selectedasset/state/alerts. Full backend readAPI/deployed sync deferred.

Allowed paths added/modified: collector/{source-policy,fed-source,store,runner}.mjs,collector/schema.sql; scripts/history-local.mjs,scripts/fed-history.mjs; lib/information/fed-view.mjs plus type declaration; components/radar/macro-timeline.tsx,components/radar/radar-feed.tsx,app/radar.css; tests/{history-foundation,fed-history,ui-components}.test.mjs,tests/browser/radar.mjs; this task,ACTIVE,docs/HISTORY_FOUNDATION.md,docs/API_CONTRACTS.md,docs/CURRENT_STATE.md; ignored work/state27 and outputs/state27-fed. Root sole writer. APIs/Vite/worker/db/package/lock/market-state modules prohibited.

Verification: before Builder independent GPT contract review; after storage/source implementation independent high-risk B review. Unit reject source/identity widening, oldschema/noDDL, future/out-of-range/publication precision/HTML/URL/entity/size/invalidRSS, bounded retries429, concurrency/dailybudget, duplicate/reversion, receipt and version identity, cross-process reopen/query/exportrestore; old foundation and state suites. Real feed→DB(1source/noassetpair applicable)→newprocessquery→rerunhead dedupe→backup→newemptyrestore→samepublicview; data artifact not fixtures. Browser existing harness: fixture5viewports, filevalidation/date/page/reset/empty/motion/focus/selectedrange/noAPI; separate actualview imported without mock information data, never fake metadata labeled real. Node/fullbuild/typecheck/relevantlint, original C0/2.6 paths regression; measurements cost/bundle, no Optimizer absent evidence. GPT final independent audit/FIX/VERIFY; DS0 unless real approved lowrisk call, not forced. Remaining owner/price-history/research/cloud gaps stay open, no push/deploy.

Root implemented approved C0 only; B0 manifest unchanged. New bounded Map, same V2 rule, Classic selected/Radar/watchlist reuse; default-order per-dimension availability, native lazy evidence, individual time/source; no rankings or new finance logic. Local-click export has explicit origin/session-edited/unverified identity and exact full symbols; no alerts/settings or upload. Production-origin browser storage is not readable by local preview, so no claim of actual owner list obtained. User asked for minimal codes while independent work continued; no answer invented.

2026-09-22 current new validation:7newNode,253totalPASS; build/typecheckPASS; scopedlintPASS except app main3error/6warning matching corresponding HEAD findings. Initial fullNode252/253 due old SSR fixture lacking new required Map/export props; added fixture inputs only, original assertions unchanged, final253PASS. New14browser checks and old2.6tenchecksPASS in isolated Edge149.0.4022.98, five viewports with actual screenshots. No live provider/production/iPhone acceptance claim. Browser reports pageerrors/warnings empty in measured scope. Commands/results/screenshots in docs/HISTORY_FOUNDATION.md and ignored outputs/state27-watchlist. First wait used wrong label and was corrected to actual V2; empty-data assertion corrected after independent audit,14checks rerun PASS.

Performance fixture only: old selected p95.0485ms,8+selected .2714ms,20+selected .4951ms;1000iterations/80bars, no FPS claim. Mainclient759428→764507bytes(+0.67%). Controlled-clock navigation/expand/export0API additions; no new provider symbols/timer. Optimizer skipped with no measured bottleneck. Financial2.6source files unchanged.

GPT independent C0 audit found2LOW test label/negative-assertion mismatches, fixed; no confirmed product finding. Stable final-diff independent VERIFY PASS:17-file candidate hashes and original B0 hashes matched; fixes, SSR fixture, source/fixture/report boundaries reviewed; auditor did not rerun coordinator tests or claim production acceptance. DS0 actual. Whole2.7 remains unfinished, no scope deletion.

Official Fed Board source investigation solved a source option: disclaimer/RSS guidance allows unmarked Board-owned information copying/distribution with attribution. RSS200(15items) and real16Sep FOMCstatement200, published18:00UTC(minuteprecision) matching2p.m.EDT; actual received22Sep15:05:16/15:08:13UTC. No thirdparty content/logo, no fakeasset mapping or oldobservedAt. Rights and small verification artifact in ignored work/state27/sources and samples; official URLs/evidence in HISTORY_FOUNDATION. This is current-vintage reconstructed source verification, not an archive DB/realUI or allSeptember coverage. SEC rejected automatedaccess; noidentity/proxy/domain bypass. Price permission and actual owner universe still open.

At the C0 checkpoint the required next B1 was scoped: shared-budget approved Board metadata adapter and real DB/query/UI path, with contract/audit before relaxing any fixture-only boundaries; real owner version needed for asset-specific collection. No fees/production actions authorized. Retain four main deliverables and separately report real sample, September coverage, cloud, persistence/restore, research and deployment.

### B1 bounded verification amendment — 2026-09-23 CST

Initial two dispatches consumed 2 requests / 19290 bytes / 0 facts; both rejected actual official RSS one-digit day spellings. Diagnostic source capture proves the mismatch; no access denial or identity bypass. Normalize only RFC optional leading zero; validate saved response offline before network. Planner permits at most TWO further single-request runs (total4 across this integration, <=1MiB response budget), for corrected ingestion then head idempotence. Durable source6/24h limit and existing canonical DB remain unchanged, no quota reset. Stop on any rejection/failure; independent contract/parser verification before further dispatch. This remains authorized low-frequency local sample validation, no fees/remote writes/production.

Receipt terminology: source diagnostics retain actual response-received time separately from archive fact acceptance. Public view firstReceivedAt/versionReceivedAt refer to first/current accepted fact in this local archive, UI says 首次入库/此版本入库. They do not claim globally first receipt, source version publication time or point-in-time knowledge. Earlier rejected diagnostic captures remain separate evidence; never backdate archive acceptance.

B readonly audit LOW diagnostic naming FIX: original saved RSS has9645 response bytes inclUTF8 BOM, TextDecoder text9642bytes. Existing bodySha256 is decoded-text SHA, not raw response SHA; preserve original artifact and explain. Future diagnostic captures use decodedTextBytes/decodedTextSha256; bytes remains actual consumed decompressed response byte count. No data/facts or quota changed.

C0 regression first rerun failed immediate DOM count after native details toggle in motion loop. Existing component mounts evidence from React onToggle asynchronously; earlier viewport cases already wait for evidence. Fix harness to waitFor visible evidence then retain original positive count assertion, no product change/timeout increase. Rerun all14C0cases; not infer success before execution.

## B1/C1 execution checkpoint — 2026-09-23 CST

Approved fixed Boardsource local pipeline done:4dispatch38580bytes incl2parsefailures,2realrecords, rerun0writes;actualtime/currentvintage, noPIT/allmonth/assetlinkclaim. Archive/publicview/newemptyrestore equal5tables + view, collectiondisabled. Node269/fullbuild/typecheck;final44targeted;33browser(9macro/14watchlist/10state26), all5desktopviewports. C0asynctoggleassertionwaitingfixed, original assertion retained. Fullreport+runbook docs/HISTORY_FOUNDATION.md B1/C1;outputs/state27-fed. Actualsourcecutoff2026-09-22T16:26:51.618Z, receipt andarchiveaccepttime separated. Prod2.6/v24unchanged; no externalfees/push/deploy/cron.

IndependentA2MEDIUMfixedbeforeBuilder;B3MEDIUMfixedverified;datefixand2LOWprovenancelabelsverified. Rootonlywriter;DS0. Optimizerskipped(noevidencebottleneck),bundle+1.03%vsC0;real2rowqueryp95~2.33ms,notFPSclaim. Remainingactualowner/pricehistory/research/longterm/cloudnotcompleted. FinalstablecandidateindependentB/C1auditVERIFYPASS;donotarchivewhole2.7.

Final independent GPT B/C1 audits VERIFY PASS on26-file candidate, manifestSHA2567ab9d03bcb2964ee3b5ddc5afbc6037deef43c99fe5a6f5fc5333e98325f194a. Both readonly; original/restore/backup SQL/view + liveproductdiff/screenshots/evidence independently checked, noopenconfirmedfinding. No auditor rerun fullNode/browserclaim. Subsequent report/index edits are documentonly; finalhash/readback separate. Remainingfour-trackgatesnotclosed; no2.7releaseclaim.

Final document-only readback matched26files/269Node/44targeted/33browser/4requests/2facts. Auditor LOW screenshot wording corrected: first viewport shows generalmacro/unverifiednotice; completeness remains belowrecordlist, not claimed in firstscreen. No product/testchange.

## Later authorized milestone publication — 2026-09-23 CST

User subsequently requested上传部署吧. Reviewed B0/C0/B1/C1 source5d759a19946310deb1afefc12db5a490f4b12af0 published as2.7Foundation/Sitesv25, native succeeded/envrevision3;GitHub uploaded. Recordtasks/archive/MR-PUBLISH-FOUNDATION27.md. This supersedes development-only deployment status, not the incomplete four-track DoD. Actualowner, licensedpricehistory/replay/research/longtermstorage remainopen; localSQLite/rawRSS/samplefiles notdeployed, no cloudcollector. No newproductionlogin/iPhonetestingclaim.
