# 当前任务卡
状态：BUILD / MR-MOBILE-VIEWPORT
负责人：协调者；本文件单写。并行子任务用 tasks/<id>.md。
配置体系任务已记录在 archive/MR-WORKFLOW-SETUP.md。

## 手机端优化收尾
- 来源：用户要求优化手机端及修复暗色图标；既有发布授权继续有效。
- 基线：87925c4fc1bce89d2d3a17837a90781bca99ee58，已上线第 16 版。
- 目标：线上只输出一个包含 viewport-fit=cover 的 viewport；保留缩放权限。
- 范围：app/layout.tsx 的 viewport 兼容配置、docs/mobile-design.md 的说明及本任务交接。
- 非目标：框架升级、依赖修改、API/认证/行情/手势逻辑变更；不提交其他窗口新增的配置文件。
- 证据：第 16 版手机布局、浅色图标、44px 触控区域、横竖屏弹窗检查通过；最终 viewport 断言发现当前输出只有 width=device-width, initial-scale=1。
- 已验证：上线拖动/双指缩放/双击复位，访问码和站主管理权限；23 项原生测试在该基线通过。
- 验收：远程构建成功、线上 metadata 检查及现有手机布局脚本通过；不重复无关测试。
- 实施：检查固定框架版本支持的 metadata 表达，最小修正后发布；不并行写入。

## 新任务填写区
- ID / 状态 / 当前角色：
- 目标及可观察验收：
- 非目标：
- 基线 commit / 审计比较范围：
- 允许修改路径：
- 禁止修改路径：
- 必要上下文（文件 + 符号，不粘贴完整文件）：
- 接口/数据库/依赖影响：
- 实施步骤与依赖：
- 相关测试命令 / 需要完整构建的理由：
- 优化指标（无明确收益则跳过）：
- 并行分工（负责人、独占路径、分支、子任务卡）：
- 交接：修改路径、已通过/未运行验证、问题、下一步：

无具体目标和 scope 时不得从此空模板推导产品改动。
完成后归档 tasks/archive/<id>.md；ACTIVE 只保留当前任务，避免累计历史上下文。
