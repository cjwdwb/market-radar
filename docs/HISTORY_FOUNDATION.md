# 2.7 information / history contracts — local foundation

Contract `history-foundation-v1`, reviewed 2026-09-22. Status: B0/C0 and B1/C1 local Board macro archive + UI verified; actual owner universe / price archive / research gates remain open. See latest B1/C1 report below; this is not completed2.7. Active card: MR-WATCHLIST-INFORMATION-HISTORY-27. Subsequent authorized release:2.7Foundation/Sitesv25/source5d759a1 on2026-09-23CST; local collectors/data were not deployed as a cloudservice. See tasks/archive/MR-PUBLISH-FOUNDATION27.md; earlier development NOT DEPLOYED remains historical.

## Source and universe contract

Current actual watchlist must come from an explicit owner export/list with acquiredAt and version. Browser defaults and monitor seeds are fixtures only. Universe asset identity includes market, venue, provider ID/symbol, quote currency, adjustment and role(asset/helper). Exact stock issuer CIK must be corroborated before association; ticker/name match alone insufficient. No inferred benchmark for unknown asset. Universe frozen per run, local-only until separately authorized.

| Source/type | Technical / rights evidence this round | Decision |
| --- | --- | --- |
| OKX history candles | Current live adapter only96 recent15m bars. Official agreement and docs requests failed TLS/certificate revocation network check; no bypass. User flags9.4 restriction; full terms not independently obtained. Native pagination/confirm/volume/day bucket still need official verification. | Historical persistence/private research/display/redistribution each UNKNOWN; no approved collection or third-party history endpoint. |
| Yahoo stock intraday | Official US terms retrieved: automated access/collection requires express prior permission; restrictions also address substitute databases/archives. Existing5m/15m/hour ranges differ and current session is not historical calendar. Account/region/product-specific permission and monthly native-minute retention still unproved. | No new historical collection approved; do not substitute unofficial library. Existing live source unchanged in this scoped task. |
| SEC submissions/filings metadata | Official API documentation request403. No alternate identity/domain/proxy used to evade it. Need fair-access/rights readback, compliant declared UA and verified current-US-asset CIK. | First preferred information candidate, NOT approved/collected yet. Metadata only proposed; filings are issuer assertions, not SEC endorsement. No document reproduction approval inferred. |
| FRED/ALFRED | Official realtime-period documentation downloaded: date vintage distinguishes what was known, default current information. No series selected, no key/series-specific rights/sample. | Limited macro Deferred pending relevance, rights and vintage evidence; date precision not intraday timestamp. |
| Federal Reserve Board monetary policy | Official disclaimer and RSS usage page verified; monetary RSS and 2026-09-16 statement returned200. Unmarked Board information is public domain, copying/distribution permitted with attribution. | Suitable narrow macro metadata source: Board-authored title, native guid/link, type, publication metadata. Exclude third-party copyrighted content, seals/logos/images/framing. Actual metadata collector → DB → export → offline UI verified in B1/C1; no hosted API or auto collection. No inferred company association, full historical coverage or PIT version proof. |
| Other markets/news/RSS | No approved provider or full-history denominator. | Deferred; no claimed HK/CN/crypto information completeness. |

Official links checked/attempted: [OKX terms](https://www.okx.com/help/okx-api-agreement), [OKX docs](https://app.okx.com/docs-v5/en/), [Yahoo US terms](https://legal.yahoo.com/us/en/yahoo/terms/otos/index.html), [SEC APIs](https://www.sec.gov/search-filings/edgar-application-programming-interfaces), [FRED realtime](https://fred.stlouisfed.org/docs/api/fred/realtime_period.html). Yahoo key language: automated collection “for any purpose without our express, prior permission.” HTTP accessibility, legal scope, sample success and complete archive are separate facts. Scope/region/account-specific rights unresolved; a password-protected site may still display to third parties. Downloaded official docs in ignored work/state27/sources, no source data collected.

Source-rights record must independently allow access, persistence, research, metadata/excerpt/fulltext display, redistribution/export and external-model disclosure. Unknown=false for execution. B0 registry was fixture-only; B1 adds only the fixed reviewed Board monetary metadata adapter below. Manual runtime cannot accept arbitrary URLs, cookies or model credentials.

## Time / provenance

- Target start:2026-09-01 in exchange IANA timezone for equities; crypto UTC. Store UTC instants plus timezone, trading date and precision. Original day-only publication retained as day; do not manufacture midnight knowledge.
- Freeze cutoff at run creation, target `[start, cutoff)`. Include bar only if open≥requested start and close≤cutoff; no incomplete bar crossing cutoff. Warmup is separately tagged; full2.6Transition needs6h close span plus1 native bar (73x5m or25x15m), at least22 continuous bars per anchor. No interpolation across session gaps.
- Time fields: occurredAt/start/end; sourcePublishedAt or null + precision + versionKnownAt/evidence; firstReceivedAt actual; fetchedAt actual; computedAt actual. No reconstructed record may claim observed_live. Store reconstruction as current-vintage unless exact point-in-time evidence exists.
- Logical bar key: source + canonical asset + interval + open + currency + adjustment. Information key: source + verified issuer/source record ID + canonical document identity. URL tracking cleanup must not merge separate amendments/files. Hash excludes receive timestamp but includes allowed fact payload/version/publication state.
- Only content equal to the current head for that logical key is an idempotent retry with stable first receipt; different content appends a monotonic revision. A→B→A appends the final A as a new revision rather than resurrecting the first A. Retracted/permission-invalid versions retained only within rights and marked unusable. Do not overwrite prior facts or claim old content was known then.

## Storage / access / API boundary

Use built-in Node SQLite for B0: file under ignored `work/state27`, no production binding, new schema only. Four logical layers in one DB: source rights/version metadata; immutable facts; derived records (later); research protocols/forecasts/outcomes (later). No R2 for initial metadata/OHLCV; no cross-store atomicity claim.

B0 tables: `archive_meta` schema version; `archive_runs` frozen owner/universe/source/range/config and persistent budget/checkpoint/status; `archive_facts` immutable row revision, owner/source/asset/type/logical key/content hash/occurredAt/sortAt/receivedAt/payload; `archive_pages` page hash and checkpoint evidence. Information at day/unknown precision has occurredAt=null. Fixture day dates explicitly UTC, sortAt=end-of-day upper bound (not publication time); unknown uses firstReceivedAt. Sort bound is excluded from content hash, query returns sortBasis, no intraday known-at claim. Real exchange timezone conversion remains adapter-gated. Additive local schema; monitor tables untouched. Rollback: stop local runner and use a new empty DB restored from verified export; no destructive down migration to production.

Store methods: createRun(config), getRun(owner,runId), reserveRequest/releaseRequest/deferRetry, commitPage(owner,runId,{expectedCursor,nextCursor,records,receivedAt,traversalDone,leaseToken}), stopRun/resumeRun, query({owner,source,asset,kind,from,to,limit,cursor}), exportSnapshot, restoreSnapshot. Owner comes from trusted local operator, not untrusted browser header. Strict schema/length/date/limit validation; parameterized SQL. Requests reserved before dispatch with persisted per-run8s lease capped at deadline; expired owner cannot commit/stop/retry over successor. Late bytes still charged; successor cannot commit beyond cumulative budget. Explicit operator stop is separate. Failed transaction rolls facts/pages/checkpoint back. Per-cursor attempt≤3 and retryNotBefore persist through restart; no resume reset/early429. Cursor and lease commit atomically with facts. This is retry-safe at-least-once retrieval, not exactly-once networking. Full market/venue/provider/currency/adjustment identity immutable within owner/source/asset ID; role/universe changes allowed.

Query fixes readRevision on first page; latest revision per logical key at/below that watermark. Stable order sortAt/id; cursor bound to filter/owner/watermark. Return records, nextCursor, revision, coverage/limitations; old data never enters live alerts. B0 coverage is explicitly not_verified with unknown denominator; run status/counters separately queryable. Source-specific gap ledger and perasset expectations remain real-B prerequisites. Max31-day query and200 rows/page; do not fetch provider in GET. New public HTTP APIs are not activated in B0. Future read routes remain authenticated and owner-scoped, no public caching; management requires separate owner and same-origin/CSRF checks, not merely site access code.

Export/restore caps:16MiB serialized bytes and10000 total rows across all tables; stat input before read/parse, reject export before writing if either cap exceeded, no partial file. Export bounded JSON carries schema and whole-payload SHA256 plus facts/runs/pages; restore into new empty DB, validate schema/checksum/identities/foreign keys/counts, transaction then compare query/checksums. SHA detects corruption, not authenticity of an untrusted export. Export contains private universe context, so local operator only and ignored by Git. Backup and DB snapshots same commit; no arbitrary SQL import.

## Collection / budget / coverage

B0 used a manual fixture-only runner; B1 source-specific exception and shared hard limits are defined below. Adapter injected in tests; no default list, network or timer. Frozen run source/universe/range/config. Production/local real adapter needs explicit source rights, identity and allowed fields. HTTP future adapters whitelist reviewed HTTPS hosts/endpoints, reject redirects, limit compressed/decompressed bytes, timeout, strip HTML, safe links only. No arbitrary URL fetch capability.

Initial approved B0 hard ceilings per run:2 assets incl helper,20 dispatch attempts,10 committed pages,2MiB/page,8MiB received total,2000 new facts,60s wall duration,concurrency1,max2 retries (3 attempts/page). Counters persisted; all retries charged before request. Received bytes including invalid/oversized/rejected pages are charged outside the facts transaction and never rolled back with a rejected page; B0 has test adapters only, future HTTP adapter must enforce streaming decompressed limits. Retry-After beyond time budget pauses instead of early retry. Cancellation and budget exhaustion save cursor/reason; no infinite resume resetting same-run budget. Fresh run requires operator decision and cannot imply approved daily allowance. Local per-day real collection NOT enabled; before real adapter: shared source/day ledger hard request/page/byte/write limits across jobs required. Frontend provider capacity reserved, no backfill uses existing live fallback/alternate hosts.

Page duplication, no-progress cursor, invalid/oversized/out-of-range record fail without advancing; transaction prevents partial-page visibility. Source failure local to job. Traversal_done says only approved endpoint traversed, not gap-free. Coverage perasset/source/type/interval/range includes submitted/valid/duplicate/revision counts and failures; expected count unknown without verified calendar/listing status. News has no universe-wide denominator. Empty page is endpoint end only if adapter proves semantics; never assume HTTP200/database rows mean complete.

## Cost model and storage lifetime

Official readback [D1 pricing](https://developers.cloudflare.com/d1/platform/pricing/) 2026-09-22: Free5M reads/day,100k writes/day,5GB total; Paid25B reads/month and50M writes/month included, extra$0.001/M reads,$1/M writes,$0.75/GB-month beyond5GB. Index writes add billable rows. No D1 egress charge; Worker compute separately billed. [Workers pricing](https://developers.cloudflare.com/workers/platform/pricing/) readback: paid minimum$5/month,10M requests+30MCPUms included; excess$0.30/M requests,$0.02/MCPUms. These are platform published tiers, not proof of account plan/remaining allowance or purchasing consent.

For sizing only (NOT actual watchlist): max20assets+4shared benchmarks,31-day full24h5m upper bound=214272 bars; 6h5m warmup adds1752=216024. Assume3 content versions ×2KiB including row/index allocation=~1.24GiB;10000metadata versions×4KiB=39MiB;≤20000derived/research×4KiB=78MiB;2 additional logical backups≈4.1GiB total. SQLite actual/index/JSON sizes measured on fixture, cannot assume D1 byte equivalence. Native15m crypto instead gives96bars/day. Expected equity count requires calendar, not this conservative envelope. No fullbody media/R2 cost in MVP.

At3versions and conservative6 SQL/index writes/version,~3.9M writes one-time; backup/read scans budgeted separately. Paid overage ceiling if no included writes left≈$3.90+storage/read/Worker usage, NOT total provider price or consent. Dispatch cost=ceil(native rows/provider verified page cap) perasset+finite overlap+retry, cap unknown until source API verified. History licensed provider costs UNKNOWN; current fixed subscriptions/remaining$350 target UNKNOWN. Local B0 incremental external/cloud/data cost$0; disk/electricity not estimated. Paid account overage is not stopped by bill alerts; application ledger hard limits required before enablement.

[D1 Time Travel](https://developers.cloudflare.com/d1/reference/time-travel/), [limits](https://developers.cloudflare.com/d1/platform/limits/) and [local development](https://developers.cloudflare.com/d1/best-practices/local-development/) retrieved after bounded TLS retry: Paid30days/Free7days recovery; perDB Paid10GB/Free500MB. The conservative archive sizing above exceeds Free perDB allowance and is not authorization to upgrade. Time Travel is not permanent version history or our completed cloud restore drill. [Import/export](https://developers.cloudflare.com/d1/best-practices/import-export-data/) retrieved: local prototyping supported; export blocks other DB requests and FTS/virtual-table restrictions apply. Local Wrangler can explicitly use remote bindings, so merely saying local is not sufficient; this B0 uses NodeSQLite without any binding instead. D1 deployment/API compatibility still untested. No remote resource created/read/migrated.

Proposed real retention: versioned approved facts retained365days subject source rights/revocation; job telemetry90days; local manual export after each successful batch. Cloud backup cadence/off-machine encrypted destination and storage region require owner approval. RPO1batch/RTO30minutes are proposed drill targets, NOT achieved SLA. Current ignored work directory is disposable development storage, not long-term custody.

## Replay / research protocol (not implemented in B0)

Historical adapter explicitly validates native5m/15m bars, finite OHLC/positive prices, ordered unique times, complete bars≤asOf, continuous suffix max80/min22, source/currency/adjustment/session proof. Unknown historical stock session = unavailable for unsupported dimension. Current quote session never backfills bar-session proof. Reuse direction/RMS helpers and unchanged v2 floors; source versions and receivedAt remain real. Relative requires original benchmark mapping and exact paired timestamps; no guessing/self benchmark. Alignment/Transition retain shared-data caveats. Cannot reconstruct old alert cooldown/lifecycle from OHLCV alone: not_reconstructible.

First protocol `same-asset-short90-forward30-v1`: same canonical asset/source/currency/adjustment/native interval/rule version; features short direction category, net%,path efficiency,RMS ratio where valid. Candidate must match direction category and RMS category; absolute net difference≤0.30 percentage points and efficiency difference≤0.20 fixed before experiment (descriptive retrieval, not trained). Optional relative excluded until comparable PIT benchmark proof. Record raw/eligible/de-duplicated counts and residual dependence. First filter eligibility, then ascending-time greedy nonoverlap, then retain at most200 by absolute net difference (ties earlier timestamp); no trading score or outcome-dependent selection.

Query timeT; candidate feature interval uses full validated dependence (including22bar gate), target+30m simple return%=100(Pend/Pstart−1). Candidate targetEnd<T and availabilityAt≤T. Candidate and query feature/target intervals cannot overlap; greedily choose earlier nonoverlapping full dependency+target intervals, including gate window, not merely 90m label. Minimum10retained samples else insufficient. No random train/test splits; initial fixed protocol no fit/threshold selection; later calibration requires earlier training segment and embargo≥max(full feature dependency,30m target). Print sample min/median/max returns only, not uncalibrated probabilities.

Without PIT revision evidence, use only `historical_simulation/current_vintage` and prominently prohibit as-then-known/performance claims; do not pass this into PIT research. Today-generated record createdAt is today, inputAsOfT separately. Prospective research append-only before result; pending before maturity, unavailable after maturity if no valid complete outcome; outcome appended independently, never rewrite scenario. Forecasting disabled until suitable data/protocol approved. Realized descriptive samples do not prove future validity.

## Delivery / verification and runbook boundary

A source rights/universe audit; B0 local schema/runner/query/restore tests; B real sample andapproved-range coverage; C bounded watchlist+timeline/history usingactual data; D deterministic replay/research tests andmaturity records. No version completion until all four tracks delivered or explicit approved scope change.

B0历史实现（早于实际B1 `scripts/fed-history.mjs`；本地Runbook见下方；其中 "No CLI collecting or daily scheduling yet" 属B0阶段事实）：Local CLI implemented: `node scripts/history-local.mjs init <new-db.sqlite>`, `status <db.sqlite> <fixture-owner> <run-id>`, `query <db.sqlite> <bounded-query-JSON>`, `export <db.sqlite> <fixture-owner> <new-backup.json>`, `restore <new-db.sqlite> <backup.json>`. All paths confined to repo work/state27; rejects symlinks/outside paths and existing init/restore targets. No CLI collecting or daily scheduling yet. Read/query never collects. Config names planned HISTORY_UNIVERSE_FILE,SEC_USER_AGENT,HISTORY_DB_PATH only; not wired, never include real values. Trusted local test code alone calls createRun/runBatch/stopRun/resumeRun; budgets/cutoff survive reopen. Correct failures before resume; exhausted budgets never reset on resume. Cloud startup/stopping/backup/credential deployment stays待授权/待启用.

## B0 execution evidence — 2026-09-22

Implemented local-only fixture schema/store/manual executor and CLI,28 new tests. Explicit `identity='fixture'` and source prefix required; no real-provider adapter exists. This is not a licensed data integration, user-universe collection or research replay implementation. Publication at day precision has no manufactured occurredAt. No account/auth/monitor/live State source code changed. Current fixture schema is a development candidate; pre-fix measurement databases are not migration targets, use final measurement folder or new DB.

Final new validation:246/246 Node tests (218 prior regression cases +28 foundation); final Vinext build, official typecheck and affected ESLint pass. Script wrapper npm test NOT RUN: same actual Windows-compatible build/typecheck/Node commands executed separately. First new budget fixture reused same source/rows from preceding case, reducing actual writes through valid dedupe; corrected fixture to separate source per budget case, assertions unchanged. Crash tests advance explicit fixture time9s before replacing expired lease; no claim real9s wait. First measurement helper import path wrong, corrected before measurement. No historical check counted as new.

Subprocess termination at after_facts/after_checkpoint rolled back transactions, retained request/byte charges; after successful commit cursor survived process exit and continuation. Separate child process loaded closed file DB, restored export into new empty DB, exact query and revision equality. Recomputed historical financial result NOT RUN: historical adapter belongs to later D, not fabricated with live gate. The restore drill currently proves archive facts/query integrity only.

Final synthetic1000 native5m bars, Node24.19, isolated file SQLite: write70.16ms,200-row query100iterations p50=1.025ms/p95=1.227ms, export14.95ms, restore69.78ms; DB671744bytes, JSON backup598999bytes. One injected adapter dispatch,0 real-source requests. Evidence `outputs/state27/measurement.json`, retained ignored `work/state27/measurement-6rDc52`. This is one local run, not cloud throughput/SLA/FPS or measured real request cost. No measured bottleneck; Optimizer skipped. Prior large client chunk warning remains; main client759428bytes matches2.6 final recorded size, new modules not imported by web bundle. No UI navigation request measurement needed/claimed in B0.

Independent GPT reviews completed: A contract1MEDIUM/1LOW fixed+verified; implementation3MEDIUM corrected; final storage2MEDIUM and runner3MEDIUM fixed (lease successor guard required secondfix). Final storage27tests+lease restore probes VERIFY PASS; final runner7targeted+2independent probes VERIFY PASS. Snapshot hashes in outputs/state27/candidate-manifest.json; root final28foundation/246suite success. These are independent bounded B0 audits, not full2.7 acceptance. DS actual0.

At the B0 checkpoint, all UI, real SEC/price samples, September coverage, PIT reconstruction, forecast maturity, cloud/cron and off-machine retention were NOT RUN/BLOCKED by their stated gates. The C0 continuation supersedes watchlist UI status; B1/C1 later supersedes the Board macro archive/UI status only. No request to old production, no deployment. Task remains open; full2.7 audit cannot pass until actual required tracks exist.

Tests must prove identity/time/cutoff/revisions/owner isolation/transaction/crash/retry/pagination/limits/restore, plus old2.6 regression before financial extraction and UI paths before integration. Existing browser harness contains simulated cookies/data, loopback only, never production. Independent GPT A andB/Dreviews plus stable-diff final review; DS0 unless actual authorized invocation occurs. Raw logs/private lists stay ignored; reports contain counts/contracts, notcredentials.

## C0 continuation and official source resolution — 2026-09-22

The watchlist can use the already loaded quote/history snapshot independently of the historical archive. `buildWatchlistState` projects the existing asset-state-v2 rule for at most20 valid unique watchlist symbols plus selected. Parent owns the single Map; selected Classic/Radar and the watchlist consume its entries. It does not add provider symbols, calls, timers or change any rule. Rows keep insertion order and individually expose90/180m Direction/RMS,90m Relative, Alignment including missing participants, and availability of five historical adjacent-window comparisons. Evidence/reasons stay behind native disclosure. Historical comparison is not persisted online state change. Empty/stale/missing data are not neutral. Classic overview is collapsed initially; Radar adds compact per-row readouts.

User-click export `browser-watchlist-v1` downloads local JSON, never posts data or starts collection. Only full symbols, explicitly app-derived market/provider routing and unresolved identity fields are included; no alerts/settings/quotes/history/cookies. `origin` distinguishes saved_browser/app_default/storage_unavailable and sessionEdited records current-session edits. Saved_browser is not proof of owner approval; identityStatus remains unverified_owner_universe. USD/USDT are not aliased by export (existing application storage migration unchanged). Empty lists are exportable. Production has no new export button until separately authorized deployment; localhost cannot see production-origin browser preferences.

Official-source breakthrough: [Board disclaimer](https://www.federalreserve.gov/disclaimer.htm) says “Unless otherwise indicated, information on Board's website is in the public domain and may be copied and distributed without permission. Please cite to the Board as the source of the information.” [RSS guidance](https://www.federalreserve.gov/feeds/feeds.htm) describes reader/aggregator access. Approval is narrowly Board-authored, unmarked text/metadata with attribution; does not extend to FRED third-party series, price archives or company filings. No Board logo/seal/framing used.

Readonly GPT source verification fetched [monetary RSS](https://www.federalreserve.gov/feeds/press_monetary.xml),200/9642bytes/15 current items at2026-09-22T15:05:16.210Z, and [statement](https://www.federalreserve.gov/newsevents/pressreleases/monetary20260916a.htm),200/82633bytes at15:08:13.347Z. Title “Federal Reserve issues FOMC statement”; guid is canonical article URL; RSS publication Wed,16Sep2026 18:00:00GMT matches page2:00p.m.EDT. Semantic precision minute; HTTP Last-Modified is not publication time. Metadata evidence saved in ignored work/state27/samples/fed-monetary-20260916-verification.json, rights text in work/state27/sources/fed-rights-node.txt. These are local verification artifacts, not a collector DB, archival UI, complete September news or observed_live history. Current-vintage reconstructed identity; no PIT content-version evidence. First-received timestamps were not backdated. Source source-options agent actually participated; DS actual0. SEC403 not retried via changed identity/domain; Yahoo/OKX archive rights remain unapproved.

New tests:7 watchlist/export Node cases; full253/253 Node PASS after updating existing SSR fixture to supply required Map/export props (initial252/253 was missing props, assertions retained). Vinext build and typecheck PASS. Scoped ESLint PASS excluding main app; app itself FAIL with3 errors/6 warnings, same rules and corresponding locations on HEAD baseline and candidate (hydration/state-in-effect plus existing hook/img/unused warnings); not called lint-clean and no unrelated cleanup. `git diff --check` PASS, line-ending notices only. No npm test wrapper execution claimed.

Existing loopback harness added RADAR_WATCHLIST27_ONLY:14 scenario checks across1440x1000,768x1024,390x844,320x740,844x390 PASS; old RADAR_STATE26_ONLY:10 checks PASS. Edge149.0.4022.98 desktop, fixture data and isolated storage; not production/iPhone/real user list. Covers shared facts, partial/empty/stale/benchmark failure, download contents/origin, remove/undo/add/persistence, keyboard/native disclosure, manual-range return, three motion preferences and storage failure. Current browser reports pageerrors=[] and captured warnings=[]; these bounded checks do not imply universal absence. Initial wait expected an incorrect label; fixed to exact V2 label. Audit also strengthened empty-state assertion to explicit samples-insufficient; final rerun14 PASS. Screenshots outputs/state27-watchlist/{desktop,tablet,mobile,narrow,landscape}-{classic,radar}.png inspected; actual local fixture captures.

Commands: `node node_modules/vinext/dist/cli.js build`; `node scripts/typecheck.mjs`; `node --experimental-strip-types --import ./tests/register-types.mjs --test tests/*.test.mjs`; `node tests/browser/radar.mjs` with RADAR_PREVIEW_URL=http://127.0.0.1:5177, local runtime PLAYWRIGHT_MODULE, respective watchlist/state26 flags and output directories. Existing preview5176 occupied; started5177 without touching it. Temporary invocation with absent Python failed before edits; used installed Node. No preview/fixture command pointed to production.

Measured Node24.19 synthetic80bar snapshot,100 warmups/1000iterations: selected-only p95=.0485ms, default8+selected=.2714ms,20+selected=.4951ms; these are different bounded workloads, not a claimed speedup or mobile FPS. Main JS759428→764507bytes (+5079,~0.67%). Controlled-clock navigation/disclosure/export added0 API requests after explicit symbol/range loads; normal polling was suspended only for isolated measurement. Domain adds0independent fetches/0symbols/0timers by diff. No measured bottleneck; Optimizer skipped. Evidence outputs/state27-watchlist/measurement.json and verification.json;2.6 regression subfolder. Original B0 five-file hash manifest unchanged.

Next actual-data contract: freeze explicit owner list/version (do not infer from defaults); use Board monetary metadata as finite general-macro context, with no invented per-company link. A real adapter must use the shared hard-budget/receipt/revision contract and receive its own B1 audit; do not weaken B0 fixture guards or claim JSON proof is ingestion. Price history requires applicable permission/provider choice before real backfill. Timeline/history UI, actual September archive, replay/research, remote retention and continuous collection remain open. No cloud resources, migration, purchase, upload or deployment this round.

Final C0 independent GPT VERIFY PASS:2LOW assertion findings corrected;17-file hash candidate matched and B0 unchanged. Auditor read test/build/lint/measurement evidence and checked diff/report boundaries, did not claim independent browser/network rerun. No unresolved confirmed C0 finding; realB/D/production remain outside this acceptance.

## B1/C1 real macro archive and offline timeline — 2026-09-23 CST

Baseline remains HEAD `00d7ef41cdfccccec32674cee6f830f6b812a45e`, branch codex/mr-watchlist-information-history-27; all earlier B0/C0 changes preserved, current candidate uncommitted. Production record remains2.6/Sitesv24/source8641d8193e5e2c27fb316bcaed9b977d63e1d119; not retested or redeployed here. GPT Planner froze the narrow source/permissions/UI contract, independent GPT A review found2MEDIUM(source-wide atomic quota/Retry-After and restore-budget rollback) and verified fixes before Builder. Root is sole writer; no new framework/dependency/Provider/DS call.

### What actually works

A manual local Node collector reads only Board monetary-policy RSS, stores permitted text metadata in SQLite, appends content revisions, queries a frozen version, exports a public view, and displays it through an explicit local-file import in Radar. General macro context is never attached to a guessed company/ticker. No articles, logos, raw XML or owner/job configuration enters the view. Existing watchlist/State/Signal math, live market sources, polling, alerts, access gate and monitor remain unchanged.

Physical SQLite schema2 adds a source-wide dispatch ledger/lease/Retry-After and collection_enabled. It rejects schema1 files before DDL; no silent migration or overwrite. Fixture logical exportv1 remains compatible; real exportv2 includes its source ledger. Restores validate facts/checkpoints/ledger and permanently disable real collection in the restored file; there is no reviewed enable command. Normal collection uses only fixed `work/state27/fed-monetary/archive.sqlite`. Failed dispatches stay charged. Default CLI run has1request; source cap6/rolling24h, at least1s between dispatches; config caps3requests/1page/256KiB/50writes/30s, lease/fencing/deadline stop concurrency and retries. No automatic job, HTTP trigger, visitor-URL input, cloud binding or server API.

Initial two approved dispatches each consumed9645bytes and accepted0facts: official RSS has valid one-digit day dates, rejected by the original stricter spelling rule. Saved source XML enabled offline diagnosis; parser now accepts optional leading zero only, retaining calendar/weekday/time/URL/XML validation. Independent parser verification passed before documented bounded amendment to at most4total requests (<=1MiB). The third request accepted2records; the fourth accepted0new records, proving head dedupe. No further real requests were made. Total4requests/38580decompressed-responsebytes,2accepted pages/2facts. Request/byte counts exclude earlier C0 official-document investigation, which remains separately reported.

### Actual range and time evidence

Frozen successful runs: `[2026-09-01T00:00:00Z, 2026-09-22T16:24:56.567Z)` and `[2026-09-01T00:00:00Z, 2026-09-22T16:26:51.618Z)`. Latest RSS has15items:2in range,13outside. Both records are published2026-09-16T18:00:00Z(minute precision): FOMC statement and September15–16 economic projections, native monetary20260916a/b links. First successful raw response received16:24:58.910Z; first accepted facts16:24:58.933Z. Earlier failed diagnostic receipt16:13:12.495Z remains separate; prior C0 source investigation is also not backfilled as archive acceptance. Public firstReceivedAt/versionReceivedAt mean first/current accepted fact in this local archive; UI explicitly says 首次入库/此版本入库. No observed_live, old online quote/fetch metadata or PIT version claim.

Diagnostic artifact response byte count includesUTF8BOM(9645); decoded text is9642bytes. The earliest artifact field bodySha256 hashes decoded text, not raw bytes; retained unchanged and documented. Later captures explicitly name decodedTextBytes/decodedTextSha256. Public view excludes diagnostics. This is traversal of the current approved RSS snapshot; completeSeptember/publication denominator is unknown. Two rows are not all-month completeness, asset-specific news, a price archive or a research result.

### Persistence, query, UI and restore

Separate CLI processes performed collect → status → view → backup → restore to a new empty `restored.sqlite`, then independent process compared all facts/runs/pages/source ledger and identical public view. Counts:2facts,4runs,2pages,1source,4dispatch records. Restored collection_enabled=0. DB61440bytes; backup7275bytes; public view1781bytes; data revision2. All stored versions/keys and query viewId match; no production deletion/recovery occurred. A first verification script referred to a nonexistent archive_coverage table and failed; corrected to actual schema archive_pages and reran full comparison, without changing data. This drill reproduces metadata query, not a financial historical recomputation (D still Deferred).

Radar section is default collapsed. On explicit JSON import it validates size<=1MiB/rows<=200, exact fields, checksum, time/range/version, duplicates and canonical HTTPS links. Checksum is integrity only, never official authenticity. UTC date filter/page5 use one immutable snapshot; changed filter/file resets page; refresh clears local state. General-macro/unverified notice stays visible; full current-vintage/time/coverage explanations are accessible in native disclosure. No upload, LS persistence, backend collection or current-State contamination. Errors/empty queries remain local. All screenshots use fixture market prices; separate real-information screenshot uses actual2-record DB export.

New test evidence and audit completion follow below.

### Validation, performance and operational handoff

This continuation ran `node --experimental-strip-types --import ./tests/register-types.mjs --test tests/*.test.mjs`:269/269 PASS (16 new Board tests + previous253). Final scoped `node --test tests/fed-history.test.mjs tests/history-foundation.test.mjs`:44/44 PASS after decoded-hash naming fix. `node node_modules/vinext/dist/cli.js build` and `node scripts/typecheck.mjs` PASS. Scoped ESLint PASS; shared main app retains previously measured HEAD3errors/6warnings, not claimed full lint-clean. npm test wrapper NOT RUN (equivalent components invoked directly; no duplicate build). Diff-check PASS. Evidence outputs/state27-fed/{node-tests,targeted,build,typecheck,lint,lint-final}.txt.

Existing loopback harness `node tests/browser/radar.mjs` on http://127.0.0.1:5178, local PLAYWRIGHT_MODULE, RADAR_OUTPUT_DIR and flags: RADAR_FED27_ONLY=1 plus RADAR_FED_REAL_VIEW=work/state27/fed-monetary/public-view.json:9scenario checks PASS; RADAR_WATCHLIST27_ONLY=1:14PASS; RADAR_STATE26_ONLY=1:10PASS. Total33checks, Edge149.0.4022.98 desktop simulation;1440x1000/768x1024/390x844/320x740/844x390. Watchlist first rerun failed a count performed immediately after asynchronous native-details onToggle; added visible-element wait then retained original assertion, all14 reran PASS. No financial or UI product change to mask test failure. Browser outputs under browser/,watchlist-regression/,state26-regression/, captured pageerrors/warnings empty in these checks. Real macro import is one additional390px check; all five fullmatrix information records are explicitly synthetic. Production/iPhone/new real user list NOT RUN; older2.45device feedback not reused.

Manual image inspection: fed-real-mobile.png (actual Board metadata, surroundingfixturemarket), fed-narrow.png and fed-mobile.png (synthetic) show readable primary titles, UTC filters, visible general-macro/unverified notice; completeness limitations remain below the record list, defaultcollapsed provenance and44px controls. Native browser scrolling retained. Reports cover date/page/reset/unsafeURL/oversize/empty/importfailure/reload,keyboard,3motionchoices and asset/manual1week return. Older watchlist/missingdata/storagefailure/statepaths passed above. Source/rightsstorage failures do not touch quote/chart flow.

Build mainclient764507→772408bytes(+7901,1.03% vs C0; +1.71% vs2.6). Existing500kBwarning persists. No dependencies/dynamic-import scatter added. Built-incrypto/file parsing and compact timeline account for scoped delta; no measured render/network bottleneck, Optimizer skipped. Actual2-record public-view query100iterations(localNode24.19,includesvalidation/digest):p50=1.5261ms,p95=2.3318ms; DB61440bytes,backup7275bytes. This is a small local workload, not throughput/large-scale/FPS assurance. Controlled-clock fixture import/filter/pagination measured0additional /api/ calls; watchlist and State navigation separately0. Polling paused only in experiment. Source collection uses4explicitCLIrequests, frontend provider symbols/timers unchanged; do not confuse those with0frontendrequests.

Runbook (local development only, no deployment):

- Stop: no scheduler/service exists; do not invoke collect. Runner finite deadlines and persisted budgets terminate each batch; operator process interruption leaves recoverable durable facts/checkpoint, later requests remain charged.
- Read existing archive: `node scripts/fed-history.mjs status`.
- New public view: `node scripts/fed-history.mjs view fed-monetary/<unused-view>.json`; UI Radar→官方宏观资料→选择文件. Browser refresh requiresreimport.
- Backup after a successful batch: `node scripts/fed-history.mjs backup fed-monetary/<unused-backup>.json`.
- Isolated restore: `node scripts/fed-history.mjs restore fed-monetary/<backup>.json fed-monetary/<unused-db>.sqlite`. Never overwrite canonicalDB; restored database is query/export-only.
- Future manual `collect` is an explicit new frozen RSS-snapshot run with dedupe, not an automatic whole-month refetch. Fixed source/dayquota, Retry-After and no-bypass remain. No CLI credential/config values needed for this public source. Do not delete/recreate DB to reset quota. Schema1 requires a separate reviewed migration, not overwrite.
- Local files remain ignored work/state27/fed-monetary; no backup uploads, off-machine retention or guaranteed permanent custody. Long-term destination/retention and any production migration/scheduler require separate authorization. Source rights changes or request failures stop collection; no alternatehost/identity fallback.

| Deliverable | Actual result | Remaining condition |
| --- | --- | --- |
| Watchlist same-source dimensions | PASS local fixtures/current2.6contract, regression rerun | Actual owner universe notprovided; export makes it available to user |
| Approved external information chain | PASS Board2records→SQLite→query/export→offlineUI | Generalmacro only, no company mapping or hostedautomaticfeed |
| September coverage | Partial: fixedstart to2026-09-22T16:26:51.618Z, current RSS2inrange/13excluded | Completenessunknown; price/source/asset archive BLOCKED pendinguniverse/rights |
| Dedupe/revision/recovery/hardlimits | PASS syntheticfailure/concurrency + realrerun0new | No unapproved fullrange or cloud collection |
| Persistence/export/restore | PASS separateprocess / newemptylocalstore, query/versionsmatch | Off-machine custody and historicalmath replay NOT RUN |
| Historical/live/researchidentity | Fixture/reconstructed guardsPASS; currentvintageexplicit | observed_live and forecast/researchimplementation NOT RUN |
| Research protocol/results | Definedonly; D Deferred | Licensed nativebars/PITconditions andrealuniverse needed; noforecast/pendingresultinvented |
| Cloud/continuous/deployment（B1历史验证） | NOT RUN | NOT AUTHORIZED; currentproduction2.6remainsup；网站后续已授权发布为2.7Foundation/v25、再2.75/v26，云/持续仍NOT RUN；源头backlog仍为tasks/MR-WATCHLIST-INFORMATION-HISTORY-27.md；发布归档见../tasks/archive/MR-PUBLISH-FOUNDATION27.md、../tasks/archive/MR-PUBLISH-PERFORMANCE275.md |

Independent B audit found3MEDIUM: atomically release sourcelease with commit, persist late429 globally afterlease succession, reject malformed/comment-injected XML. All fixed + independently VERIFIED, plus narrow datecompatibility verified against savedrealRSS. Two subsequent LOW provenance labels fixed (acceptedfact vs earlierrawreceipt; decodedhash vs rawbytecount). A/B review evidence and final stable-candidate verification below. Final stable-product independent B/C1 VERIFY PASS, no openconfirmed finding in this bounded slice. DS actualcalls0; GPT handled core and independent audits. No purchase, cloudwrite, productionconfiguration change, push/merge/tag/deploy.

Final independent GPT audits (separate readonly agents): B auditor verified baseline00d7ef4/26-file candidate manifestSHA2567ab9d03bcb2964ee3b5ddc5afbc6037deef43c99fe5a6f5fc5333e98325f194a, independently compared original/restored/backup five tables and regenerated identical publicView from both DBs; in-memory realbackup restore rejected collection. C1 auditor inspected final390real/320/desktop screenshots, independent readonlySQL matched2records with viewhash/version/time, verified browserreportviewId and retained C0assertion. Both reviewed evidence, did not claim independently rerunning full269/build/browser. No remaining confirmed B/C1finding; earlier3MEDIUM/2LOW fixed/verified. Documentation completion after that manifest does not change audited product hashes; final document-only readback recorded in task. Not whole2.7, not production acceptance.
